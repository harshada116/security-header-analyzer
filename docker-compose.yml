version: "3.9"
services:
  header-analyzer:
    build: .
    ports:
      - "5000:5000"
    environment:
      - HEADER_ANALYZER_SECRET_KEY=${HEADER_ANALYZER_SECRET_KEY:?set a random secret key}
    restart: unless-stopped
