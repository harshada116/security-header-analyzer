# Copy to .env and fill in a real random value before running docker-compose.
# Generate one with: python3 -c "import secrets; print(secrets.token_hex(32))"
HEADER_ANALYZER_SECRET_KEY=change-me-to-a-random-64-char-hex-string
